import Delir from './Delir'

describe('Delir', () => {
    it('#constructor', () => {
        const ctx = new Delir()
    })

    it.skip('#setProject', () => { })
    it.skip('#setProject should remove old components', () => { })
})

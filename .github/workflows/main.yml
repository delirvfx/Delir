name: CI

on: [push]
env:
  CXX: g++-4.8

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macOS-10.14, windows-2016, ubuntu-latest]

    steps:
      - uses: actions/checkout@v1
      - name: Setup Node.js for use with actions
        uses: actions/setup-node@v1.1.0
        with:
          node-version: '11.x'
      - name: Cache
        uses: actions/cache@v1.0.0
        with:
          path: ~/.cache/yarn
          key: yarn-${{ matrix.os }}-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            yarn-${{ matrix.os }}-
      - name: Install apt deps
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: sudo apt-get install g++-4.8 libfontconfig1-dev
      - name: Install yarn deps
        run: |
          yarn install
          yarn install -s --no-optional
          # Remove problem optional module
          rm -rf ./node_modules/speaker
      - name: Build @delirvfx/core
        run: yarn prepublishOnly
      - name: Build storybook
        run: yarn build:sb
      - uses: actions/upload-artifact@master
        with:
          name: build-${{ matrix.os }}
          path: |
            packages/delir/storybook-static
  testing:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1.1.0
        with:
          node-version: '15.6'
      - name: Cache
        uses: actions/cache@v2
        with:
          path: ~/.cache/yarn
          key: yarn-${{ hashFiles(format('{0}{1}', github.workspace, '/yarn.lock')) }}
          restore-keys: |
            yarn-
      - name: Install apt deps
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: sudo apt-get install g++-4.8 libfontconfig1-dev
      - name: Install yarn deps
        run: |
          yarn install
          yarn install -s --no-optional
          # Remove problem optional module
          rm -rf ./node_modules/speaker
      - name: testing @delirvfx/core
        run: |
          yarn test:core
          yarn test:front
          yarn test:plugins
          yarn test:contrib-pep
